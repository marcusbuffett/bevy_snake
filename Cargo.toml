[package]
name = "bevy_snake"
version = "0.6.0"
authors = ["Marcus Buffett <marcusbuffett@me.com>"]
edition = "2021"

[dependencies]
bevy = {version = "0.8", features = ["dynamic"]}
rand = "0.8"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3